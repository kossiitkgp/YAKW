---
import KOSSLogoLight from "../assets/koss-logo-light-gradient.svg";
---

<section id="about" class="section-container">
  <div class="section-wrapper">
    <img class="section-img" src={KOSSLogoLight} alt="KOSS Logo" />
    <div class="section-content">
      <div class="section-title">
        <img class="section-title-img" src={KOSSLogoLight} alt="KOSS Logo" />
        <h1>Kharagpur Open Source Society</h1>
      </div>
      <p class="section-description">
        We are a group of Open Source Enthusiasts who focus on something more preliminary and relevant, 'A Love for
        Coding'. Every year KOSS plans to hold events for familiarizing students with UNIX tools, Linux environment, Git
        Development Workflow, practice Algorithms and Data Structures, besides conducting workshops on GUI programming,
        Web development, Android application Development and Hackathons.
      </p>
    </div>
  </div>
</section>

<style>
  section {
    height: 100dvh;

    display: flex;
    align-items: center;
  }

  .section-container {
    container: Section / inline-size;
  }

  .section-wrapper {
    display: flex;
    flex-direction: row;
    align-items: center;

    gap: 3rem;
    width: 75%;
    margin: 0 auto;
  }

  .section-img {
    width: max(50cqw, 50rem);
  }

  .section-title-img {
    display: none;
    width: max(30cqw, 4rem);
  }

  .section-title {
    font-size: max(2cqi, 1.1rem);
    color: var(--accent-color-red);
    text-align: left;

    margin-bottom: 2rem;

    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .section-description {
    font-size: max(1.3cqi, 1rem);
    color: var(--text-secondary);
  }

  @container Section (max-width: 768px) {
    .section-wrapper {
      width: 80%;
    }

    .section-img {
      display: none;
    }

    .section-title {
      align-items: center;
      text-align: center;
    }

    .section-title-img {
      display: block;
    }

    .section-description {
      padding-right: 0;
    }
  }

  @container Section (min-width: 481px) and (max-width: 768px) {
    .section-title {
      font-size: 3.2cqi;
    }

    .section-description {
      font-size: 2.8cqi;
    }
  }

  @container Section (max-width: 480px) {
    .section-title {
      font-size: 4cqi;
    }

    .section-description {
      font-size: 4cqi;
    }
  }
</style>
